env: "local"  # local, dev, prod и другие
storage_path: "./storage/storage.db" # Где хранится база данных
http_server:
  address: "localhost:8082"
  timeout: 4s # Время на чтение запрос и такое же время на отправку. На обработку не ограничено
  idle_timeout: 60s  # Время жизни соединения с клиентом
  user: "i.d.furman"